apiVersion: v1
kind: Service

metadata:
  name: ai-service
  namespace: healthconnect

spec:
  type: ClusterIP

  selector:
    app: ai-service

  ports:
  - protocol: TCP
    port: 80
    targetPort: 5004