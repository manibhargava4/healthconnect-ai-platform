apiVersion: v1
kind: Service

metadata:
  name: lab-service
  namespace: healthconnect

spec:
  type: ClusterIP

  selector:
    app: lab-service

  ports:
  - protocol: TCP
    port: 80
    targetPort: 5002