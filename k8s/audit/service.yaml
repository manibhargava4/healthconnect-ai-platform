apiVersion: v1
kind: Service

metadata:
  name: audit-service
  namespace: healthconnect

spec:
  type: ClusterIP

  selector:
    app: audit-service

  ports:
  - protocol: TCP
    port: 80
    targetPort: 5003