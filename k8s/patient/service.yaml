apiVersion: v1
kind: Service

metadata:
  name: patient-service
  namespace: healthconnect

spec:
  type: NodePort

  selector:
    app: patient-service

  ports:
  - port: 5001
    targetPort: 5001
    nodePort: 30001