apiVersion: v1
kind: Service

metadata:
  name: monitoring-service
  namespace: healthconnect

spec:
  type: ClusterIP

  selector:
    app: monitoring-service

  ports:
  - port: 5000
    targetPort: 5000