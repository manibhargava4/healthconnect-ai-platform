apiVersion: v1
kind: Service

metadata:
  name: monitoring-service
  namespace: healthconnect

spec:
  type: ClusterIP

  selector:
    app: monitoring-service

  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000